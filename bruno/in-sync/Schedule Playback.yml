info:
  name: Schedule Playback
  type: http
  seq: 9

http:
  method: POST
  url: "{{SERVER_PROTOCOL}}://{{SERVER_DOMAIN}}:{{SERVER_PORT}}/in-sync-api/game/:gameCode/playback/schedule"
  params:
    - name: gameCode
      value: "{{gameCode}}"
      type: path
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        async function acknowledgeSchedule() {
          await bru.runRequest("Get Schedule");
          await bru.runRequest("Acknowledge Schedule");
        }
        if (res.status === 201) {
          acknowledgeSchedule();
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
