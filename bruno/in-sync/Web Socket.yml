info:
  name: Web Socket
  type: websocket
  seq: 2

websocket:
  url: "{{WS_PROTOCOL}}://{{SERVER_DOMAIN}}:{{SERVER_PORT}}/in-sync?gameCode={{gameCode}}&playerName={{playerName}}"
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
