info:
  name: Connect
  type: websocket
  seq: 5

websocket:
  url: "{{WS_PROTOCOL}}://{{SERVER_DOMAIN}}:{{SERVER_PORT}}/in-sync-api?gameCode={{gameCode}}&playerName={{playerName}}"
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
