spring.application.name=in-sync
spring.profiles.active=prod

# Server
spring.web.error.include-stacktrace=never

# Logging
logging.file.name=server/logs/in-sync.log

# In Sync
insync.max.games=1
insync.player.wait.time=2000
insync.schedule.offset.time=2000
insync.game.delete.time=60000